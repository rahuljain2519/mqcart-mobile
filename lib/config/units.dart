// Pack-size units for simple (non-variant) products. Mirrored in
// src/lib/units.ts on the web app — keep them identical.
const List<String> kUnitTypes = ['g', 'kg', 'ml', 'L', 'pcs', 'pack', 'dozen'];
